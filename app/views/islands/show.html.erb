  <% if @island.photo.attached? %>
        <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.2),rgba(0,0,0,0.2)), url('<%= cl_image_path @island.photo.key %>');">
  <div class="container">
    <h1>Welcome to <%= @island.location %>  🌴🌴 </h1>
  </div>
</div>
      <% else %>
        <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.2),rgba(0,0,0,0.2)), url('https://img.theculturetrip.com/wp-content/uploads/2018/04/shutterstock_1050077834.jpg');">
  <div class="container">
    <h1>Welcome to <%= @island.location %>  🌴🌴 </h1>
  </div>
</div>
<% end %>

<div class ="container" style="margin: 0 auto">

<h1><%= @island.name %></h1>

<p><%= @island.description %></p>

<p>Price per night: £<%= @island.price_per_night %></p>

<h4>Features of the island</h4>

<div class ="container show-features">
          <div class="show-card-content">
            <div class="container">
                <i class="material-icons mr-2">people</i><span><%= @island.max_guests %></span>
            </div>
          </div>

          <div class="show-card-content">
            <div class="container">
                <i class="material-icons mr-2">bed</i><span>5</span>
            </div>
          </div>

           <div class="show-card-content">
            <div class="container">
                <i class="material-icons mr-2">pool</i><span>2</span>
            </div>
          </div>
        </div>


<div id="book-button"><%= link_to "Book Now", new_island_booking_path(@island) %></div>
</br>
  <% if current_user == @island.user %>
    <%= link_to 'Edit', edit_island_path(@island) %>
    </br>
    <%= link_to 'Delete island', island_path(@island), method: 'delete' %>
    <% else %>
    <h4>hello</h4>
  <% end %>
</div>

<div class="container">
    <div id="map"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
  

<div class="container review">
<h3>Reviews</h3>
   <ul class="list-group">
    <% @island.reviews.each do |review| %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <span class="badge badge-primary badge-pill">
          <% counter = 0 %>
          <% 5.times do %>
            <i class="fa <%= review.rating > counter ? 'fa-star' : 'fa-star-o' %>"></i>
            <% counter += 1 %>
          <% end %>
        </span>
        <%= review.content %>
      </li>
      <hr>
    <% end %>
  </ul>

</div>
