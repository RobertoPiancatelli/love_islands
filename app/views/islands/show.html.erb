  <% if @island.photo.attached? %>
        <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.2),rgba(0,0,0,0.2)), url('<%= cl_image_path @island.photo.key %>');">
  <div class="container">
    <h1>Welcome to <%= @island.location %></h1>
  </div>
</div>
      <% else %>
        <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.2),rgba(0,0,0,0.2)), url('https://img.theculturetrip.com/wp-content/uploads/2018/04/shutterstock_1050077834.jpg');">
  <div class="container">
    <h1>Welcome to <%= @island.location %></h1>
  </div>
</div>
<% end %>

<div class ="container">

<div class ="show-header">

<h1 class="m-0"><%= @island.name %></h1>

 <% if current_user == @island.user %>
  <div class="container-buttons">
    <div id="edit-button"><%= link_to 'Edit', edit_island_path(@island) %></div>
    <span id="delete-button"><%= link_to 'Delete island', island_path(@island), method: 'delete' %></span>
    </div>
    <% else %>
    <%= link_to "Book Now", new_island_booking_path(@island), id: "book-button" %>
  <% end %>
  </div>

</br>

<p>Island hosted by XXXX</p>

<p>Price per night: £<%= @island.price_per_night %></p>

<p style="color:lightgray">___________________________________________________________________________</p>
</br>
<h3>What to expect from your stay?</h3>
</br>
<p><%= @island.description %></p>

<p style="color:lightgray">___________________________________________________________________________</p>

<h4>Features of the island</h4>

<ul class="show-features">
        
  <li><i class="material-icons mr-2">people</i>Island can have maximum <%= @island.max_guests %> guests
  </li>

<li><i class="material-icons mr-2">bed</i>This island has 5 bedrooms</li>

<li><i class="material-icons mr-2">pool</i>This island has a pool</li>

<li><i class="material-icons mr-2">fitness_center</i>This island has a gym</li>
  
</ul>

<p style="color:lightgray">___________________________________________________________________________</p>

    <div id="map"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
    </br>

<h3>Reviews</h3>
<div class="reviews">
    <% @island.reviews.each do |review| %>
    <div class="review">
    <p><%= cl_image_tag current_user.photo.key, class: "avatar dropdown-toggle" %> Review by USERNAME</P>
        <div class="badge badge-primary badge-pill">
          <% counter = 0 %>
          <% 5.times do %>
            <i class="fa <%= review.rating > counter ? 'fa-star' : 'fa-star-o' %>"></i>
            <% counter += 1 %>
          <% end %>
        </div>
        <%= review.content %>
        </div>
    <% end %>
    
</div>
</div>
